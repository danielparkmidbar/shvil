/**
 * 내장 코스·엔젤 데이터 — packages/shared로 이동 (서버 배포 원본과 동일).
 *
 * `SHVIL_ISRAEL`이 실제 배포되는 이스라엘 국립 트레일(OSM rel 282071, 1,055km)이고,
 * `SHVIL_ISRAEL_NORTH_SAMPLE`은 배포되지 않는 테스트 픽스처다 — 앱 코드에서
 * 쓰지 마라(실제 트레일에서 1km 이상 떨어져 있다).
 */
export { BUNDANG_BULGOKSAN_SAMPLE, SAMPLE_ANGELS, SHVIL_ISRAEL, SHVIL_ISRAEL_NORTH_SAMPLE } from '@shvil/shared';
