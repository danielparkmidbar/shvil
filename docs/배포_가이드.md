# 배포 가이드 — 웹(Cloudflare Pages) + 서버(Render 무료)

대상: 다니엘 쌤 · 목적: 닫힌 시험용 배포. **실제 업로드는 다니엘 쌤이 수행**한다.

시스템은 3덩어리다: **shvilangel.org**(정적 웹) · **shvilist.org**(정적 웹) · **디렉토리 서버**(Node).
웹 두 사이트는 디렉토리 서버 API를 소비한다 — **서버를 먼저 띄우고 그 주소를 웹 빌드에 넣는다.**

---

## 0. 준비: GitHub에 저장소 올리기

Cloudflare Pages·Render 둘 다 GitHub 저장소를 연결하는 방식이 가장 쉽다.

```
cd shvil-dev
git remote add origin https://github.com/<계정>/shvil-dev.git
git push -u origin master
```

(비공개 저장소여도 됨. Cloudflare·Render 모두 비공개 연결 지원.)

---

## 1. 먼저 서버 — Render 무료 ✅ 배포 완료 (2026-07-20)

**운영 URL: `https://shvil-directory.onrender.com`** (커밋 85cb5e7 기준, 무료 티어)
확인: `https://shvil-directory.onrender.com/transparency/promo` → JSON 응답.
이 주소는 이미 `apps/wallet/eas.json`에 반영되어 있다. 아래는 재배포·이전 시 절차다.

### ★★ 0단계 (2026-07-27 추가) — 배포보다 **먼저** 시드를 넣는다

**이걸 안 하면 서버가 뜨지 않는다.** 일부러 그렇게 만들었다 — 시드 없이 뜨면 서버가
조용히 발행 키를 새로 만들고, 이미 설치된 폰의 동기화가 영구히 끊기기 때문이다.

화면 그대로의 상세 절차는 **`docs/Render_설정_절차.md`**에 있다. 요약:

1. 내 컴퓨터에서 한 줄 실행:
   ```
   cd shvil-dev
   node tools/시드생성.mjs
   ```
   → 64자짜리 hex 한 줄과, **배포 열쇠 이름·지문** 두 줄이 함께 나온다.
2. **세 줄 모두 종이에 적는다.** 시드는 잃어버리면 되찾을 방법이 없다. 사진·채팅·메일 금지.
   (이름·지문은 비밀이 아니다 — 확인에 쓰는 값이다.)
3. Render 대시보드 → 서비스 `shvil-directory` → **Environment** →
   **Add Environment Variable** → Key `SHVIL_ROOT_SEED`, Value = 적은 값 → **Save**.
4. 저장하면 자동으로 다시 배포된다.
5. 확인: `https://shvil-directory.onrender.com/health` →
   **`distKeyId`·`distKeyFingerprint`가 종이의 두 줄과 같은지** 본다.

★★**`keySource: SEED` 와 `warnings: []` 만으로는 확인이 되지 않는다.** 시드를 붙여넣다
한 글자를 틀려도 서버는 정상 기동하고 그 두 값도 정상으로 나온다(적대검증 2026-07-28 ⑤가
실프로세스로 재현). **오타를 잡는 것은 오직 종이와의 이름·지문 대조뿐이다.**
기계가 대신 잡게 하려면 환경변수 `SHVIL_EXPECT_DIST_KEY_ID`에 종이의 이름을 넣는다 —
그러면 다른 값이 나올 때 서버가 아예 뜨지 않는다(그 값은 비밀이 아니다).

★한 번 넣은 시드는 **바꾸지 않는다.** 바꾸는 것은 발행자를 갈아치우는 것과 같다.

★★**유출 대응에 대한 정정 (2026-07-28).** 예전 판에 "유출되면 시드는 그대로 두고
`SHVIL_KEY_GENERATION`을 1 올린다"라고 적혀 있었다. **그것은 틀렸다.** 유출된 것이
**시드**라면 유출자가 같은 시드로 세대 1·2·3을 그대로 유도한다(적대검증 ②-e가 위조
증서로 재현). 세대 회전이 뜻이 있는 경우는 "파생 키 하나만 샜고 시드는 안전할 때"인데,
이 설계에는 그런 경로가 사실상 없다. 시드가 샜다면 **새 시드 = 새 발행자**이며, 그것은
문서 갱신·지갑의 「서버 열쇠」 수락·옛 코인의 처리까지 딸린 큰 결정이다.
자세한 것은 `docs/서버_키_지속성.md` 6장.

---

1. https://render.com 가입 → **New +** → **Blueprint** 선택.
2. 저장소 연결 → 루트의 `render.yaml`을 Render가 자동 인식(서비스 이름 `shvil-directory`).
   - `SHVIL_KEK`은 Render가 안전한 랜덤 값을 자동 생성한다(보안 감사 H-2).
   - **`SHVIL_ROOT_SEED`는 자동 생성되지 않는다** — 위 0단계에서 직접 넣는다(`sync: false`).
   - `SHVIL_DEV_MODE=1`로 설정되어 있다(닫힌 시험용 — 아래 ⚠ 참고).
3. 배포 완료 후 URL 확인: `https://shvil-directory-XXXX.onrender.com`
4. 동작 확인 — 브라우저나 터미널에서:
   ```
   https://shvil-directory-XXXX.onrender.com/health
   → {"ok":true,"keySource":"SEED",...,"warnings":[]} 가 나오면 성공
   https://shvil-directory-XXXX.onrender.com/transparency/promo
   → {"registrationIssued":0,...}
   ```
   (무료 티어는 15분 유휴 후 슬립 → **첫 요청이 ~30초 지연**. 정상이다.)

**⚠ 무료 티어 데이터 초기화:** Render 무료는 디스크가 임시(ephemeral)라 **재시작·재배포 때 SQLite가 초기화**된다.
- ✅ **발행 키는 이제 살아남는다** — `SHVIL_ROOT_SEED`에서 유도하므로 DB와 무관하다(2026-07-27).
- ❌ **원장은 여전히 죽는다** — 회원 명부·회원 증서 기록·지갑 백업·마켓 에스크로·엔젤 보너스
  수량 카운터·스팟 예치 회계는 재시작마다 사라진다. 같은 전화번호로 다시 가입하면 **새 회원
  번호**가 나온다. 이건 시드로 안 고쳐진다 — 유료 디스크나 외부 DB가 필요한 별도 결정이다.

---

## 2. 웹 두 사이트 — Cloudflare Pages

두 사이트를 **각각** Pages 프로젝트로 만든다. 설정은 동일하고 디렉토리만 다르다.

### shvilangel.org

1. https://dash.cloudflare.com → **Workers & Pages** → **Create** → **Pages** → **Connect to Git** → 저장소 선택.
2. 빌드 설정:
   | 항목 | 값 |
   |---|---|
   | Framework preset | None (또는 Next.js Static HTML Export) |
   | Build command | `npm install && npm run build -w @shvil/web-angel` |
   | Build output directory | `apps/web-angel/out` |
   | Root directory | (비움 — 모노레포 루트) |
3. **환경 변수** (Settings → Environment variables) — 빌드에 서버 주소를 인라인해야 한다:
   ```
   NEXT_PUBLIC_DIRECTORY_URL = https://shvil-directory-XXXX.onrender.com
   ```
   (1번에서 받은 Render 서버 URL. 이 값이 빌드 시 웹에 박힌다 — 서버 URL이 바뀌면 재빌드.)
4. 배포 → `https://shvilangel-XXXX.pages.dev` 접속.

### shvilist.org

같은 절차, 값만 교체:
- Build command: `npm install && npm run build -w @shvil/web-list`
- Build output directory: `apps/web-list/out`
- 환경 변수: 동일한 `NEXT_PUBLIC_DIRECTORY_URL`

### 커스텀 도메인 (선택)

각 Pages 프로젝트 → Custom domains → `shvilangel.org` / `shvilist.org` 연결(도메인을 Cloudflare에 두면 자동).

---

## 3. 확인

- 웹 접속 → 우상단 언어 스위처로 **עברית(히브리어) 선택 → 전체 레이아웃이 우→좌(RTL)로 뒤집히는지**.
- 엔젤 지도·마켓·투명성 페이지가 서버 데이터를 불러오는지(서버가 슬립이면 첫 로드가 느릴 수 있음).
- 서버가 비어 있으면 "아직 없음"으로 표시된다(정상 — 앱으로 엔젤을 등록해야 채워짐).

---

## 3. 지갑 앱 — Android APK (닫힌 시험)

홈페이지(shvilist.org)의 "지갑 다운로드" 버튼은 **GitHub Releases 최신 릴리스**
(`https://github.com/danielparkmidbar/shvil/releases/latest`)로 연결되어 있다.
APK를 릴리스로 올리면 홈페이지 재배포 없이 버튼이 항상 최신을 가리킨다.
**iOS는 웹 다운로드가 불가**(애플 정책 — TestFlight + 개발자 계정 $99/년 필요) →
닫힌 시험은 Android만.

빌드 준비는 코드에 **이미 완료**되어 있다: `eas.json`의 두 프로필 모두
`EXPO_PUBLIC_DIRECTORY_URL = https://shvil-directory.onrender.com`(2026-07-20 배포
완료분)을 가리키고, `app.json`에 `android.package`·Expo 프로젝트 링크가 들어 있다.
서버 주소가 바뀌면 `eas.json`만 고쳐 재빌드하면 된다(앱 안에서 사용자가
더보기 → 가입/설정 → 서버 주소로 덮어쓸 수도 있다).

**다니엘 쌤이 할 일은 아래 3단계** (~5분 + 클라우드 빌드 대기 ~15분):

```
① cd apps/wallet
   npx eas build -p android --profile preview
   (최초 1회만 npx eas login — Expo 계정 shvil-coin)
   → 빌드가 끝나면 터미널에 QR + APK 다운로드 링크가 뜬다
② APK 파일을 내려받아 GitHub 릴리스로 올리기:
   https://github.com/danielparkmidbar/shvil/releases/new
   → 태그 예: wallet-v0.1.0 · 파일 첨부: shvil-wallet.apk → Publish
③ 홈페이지 버튼 확인 — shvilist.org 첫 화면 "지갑 다운로드" → APK 내려받아짐
```

**★출시 스위치는 ②다.** 릴리스가 하나도 없으면 홈페이지의 다운로드 버튼은 빈
릴리스 페이지로 가므로, **웹을 먼저 공개해도 앱은 잠긴 상태**다. 실기기 테스트를
마친 뒤 ②를 하는 순간 다운로드가 열린다 — 그때 웹을 다시 배포할 필요는 없다.

**⚠ 주의:**
- **저장소가 비공개면 시험단이 릴리스에 접근할 수 없다.** 공개로 전환하거나(코드
  공개에 문제없음 — 비밀키는 저장소에 없다), 비공개 유지가 필요하면 APK를 다른
  채널(드라이브 링크 등)로 나누고 홈페이지 링크를 그 주소로 바꾼다.
- 서버 주소를 잘못 넣고 빌드해도 복구 가능: 앱의 **더보기 → 가입/설정 → 서버 주소**
  에서 사용자가 직접 바꿀 수 있다 (kv 오버라이드).
- Android가 "출처를 알 수 없는 앱" 설치 확인을 묻는다 — 닫힌 시험에선 정상이며,
  공개 배포 단계에서 Play Store 등록(`--profile production` + AAB)으로 넘어간다.

---

## ⚠ 지금 배포의 한계 (파일럿 착수 전)

1. **`SHVIL_DEV_MODE=1`은 닫힌 시험 전용이다.** 이 모드는 가입 OTP 코드를 응답에 노출한다(실 SMS 미연동 때문). 아는 사람끼리 시험할 때만. **공개 홍보 전에는 `0`으로 바꾸고 실 SMS/이메일을 연동**해야 한다(보안 감사 C-1·M-1·M-5).
2. **실제 가입→걷기→지불**은 웹이 아니라 **폰 앱**에서 일어난다. 웹은 지도·마켓·투명성 열람용이다. 앱은 위 3장의 APK 빌드로 배포한다.
3. 파일럿 필수 조치의 운영 항목(실 Play Integrity 토큰·TLS 인증서 피닝·KMS)은 공개 운영 전 마무리 대상이다(`docs/보안감사_M4.md`).

---

## 요약 순서

```
① git push (GitHub)
② Render Blueprint 배포 → 서버 URL 확보
③ Cloudflare Pages ×2 (web-angel / web-list), NEXT_PUBLIC_DIRECTORY_URL에 서버 URL
④ 접속 확인 (RTL·데이터 로드)
⑤ 지갑 APK: eas.json에 서버 URL → eas build → GitHub Release 업로드 (3장)
```
