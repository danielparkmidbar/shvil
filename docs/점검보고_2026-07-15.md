# 야간 전체 점검 보고 — 2026-07-15

> 다니엘 쌤 취침 전 지시("전체적으로 모두 점검해줘")에 따른 결과.

## 발견 문제와 조치 (전부 해결)

| 문제 | 원인 | 조치 |
|---|---|---|
| "디렉토리 서버에 연결할 수 없습니다" | 서버는 정상. 무료 서버가 15분 유휴 후 잠들어 첫 응답이 ~50초인데 웹 타임아웃이 8초 | ① 웹 fetch 재시도 8s→30s→60s ② GitHub Actions 10분 keepalive 핑 — 애초에 잠들지 않게 |
| 푸터 상호 링크 죽음 | apex(www 없는) 도메인 미연결 + 링크가 apex를 가리킴 | 8곳(4개 언어×2사이트) www로 수정. 현재 사이트는 수정 전 빌드 — 아침 업로드로 반영 |
| 핀-원 불일치 | 원 반경(500m) < 눈금 오프셋 최대(~720m) + 드래그 중 미갱신 | 반경 800m + 드래그 실시간 추종 |

## 점검 결과 (녹색)

- 서버 살아있음 + 최신 코드 배포 확인 (한국어 note 제거·M6 available 필드 반영)
- 공개 엔드포인트 5종 200 OK (promo/market/regions/courses/angels)
- 테스트 301개 전부 통과, 타입체크 5개 워크스페이스 통과
- 빌드 산출물 검증: www 링크 정상, 옛 apex 링크 잔존 0
- push 완료 (커밋 9d3de9b, 6b0ea05) — Render 자동 재배포

## 아침에 하실 일

1. **Cloudflare 업로드** (핵심 — 이걸 해야 위 수정들이 사이트에 반영됩니다):
   - shvilangel 프로젝트 ← `쉬빌코인\shvil-dev\apps\web-angel\out` (78개)
   - shvilist 프로젝트 ← `쉬빌코인\shvil-dev\apps\web-list\out` (96개)
2. (권장) Cloudflare 각 프로젝트 → Custom domains → www 없는 `shvilangel.org`/`shvilist.org` 추가
3. (권장, 이전부터 대기) GitHub 저장소 Private 전환 — Settings 맨 아래 Change visibility

## 다음 개발 순서 (T-5 확정)

M9 몸 인증 + 보물 마이닝 → M7 별점 → M10 QR 스팟 → M11 자전거 → M8 동행
